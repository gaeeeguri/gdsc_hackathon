spring:
  # in-memory database for testing
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:


debug: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /api-doc